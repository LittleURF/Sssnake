// Ranked leaderboard table — Rank, Snake Name, Player, Score, Date columns.
// Rank 1/2/3 rows show gold/silver/bronze badges.
// Current user's row is highlighted with an accent background.
// Props: entries (HighscoreEntry[]), currentUserId.

export function LeaderboardTable() {
  return null;
}
