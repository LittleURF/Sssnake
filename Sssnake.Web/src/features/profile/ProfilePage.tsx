// Entry-point component for the /profile route.
// Composes profile header, StatsGrid, snake appearance picker, achievements.
// Drives useProfile.

export function ProfilePage() {
  return (
    <div className="p-8">
      <p className="text-text-muted">Profile — in progress</p>
    </div>
  );
}
